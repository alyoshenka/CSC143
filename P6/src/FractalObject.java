import java.awt.Graphics;

/** represents a fractal object */
public interface FractalObject {

    /**
     * draw this object
     *
     * @param g the graphics to draw on
     */
    void draw(Graphics g);
}
